{
  "name": "test-project",
  "version": "1.0.0",
  "description": "A dummy project to test the cloud deployer",
  "scripts": {
    "build": "echo '🚧 Building...' && if not exist dist mkdir dist && copy index.html dist\\index.html"
  },
  "dependencies": {
    "fake-lib": "file:"
  }
}